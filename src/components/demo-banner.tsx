export function DemoBanner() {
  return (
    <div className="rounded-xl border border-[var(--accent)]/20 bg-[var(--accent)]/10 p-4 text-sm leading-6 text-emerald-50">
      Demo mode is using static portfolio data. It shows the same analysis workflow without depending on GitHub API availability.
    </div>
  );
}
